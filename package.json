{
  "name": "casomira",
  "version": "0.9.6-beta",
  "description": "Časomíra — správce závodu autokros / rallycross",
  "author": "Časomíra",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "https://github.com/Vituhlos/casomira.git"
  },
  "homepage": "https://github.com/Vituhlos/casomira#readme",
  "bugs": {
    "url": "https://github.com/Vituhlos/casomira/issues"
  },
  "main": "./out/main/index.js",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "preview": "electron-vite preview",
    "postinstall": "electron-builder install-app-deps",
    "rebuild": "electron-rebuild -f -w better-sqlite3",
    "typecheck:node": "tsc --noEmit -p tsconfig.node.json",
    "typecheck:web": "tsc --noEmit -p tsconfig.web.json",
    "typecheck": "npm run typecheck:node && npm run typecheck:web",
    "dist": "npm run build && electron-builder --config electron-builder.yml",
    "dist:win": "npm run build && electron-builder --win --config electron-builder.yml --publish never",
    "dist:win:dir": "npm run build && electron-builder --win dir --config electron-builder.yml --publish never",
    "dist:mac": "npm run build && electron-builder --mac --config electron-builder.yml --publish never -c.npmRebuild=true",
    "dist:mac:dir": "npm run build && electron-builder --mac dir --config electron-builder.yml --publish never",
    "docs:word": "npx --yes @mohtasham/md-to-docx docs/user/INSTALACE-MAC.md docs/user/Casomira-instalace-Mac.docx && npx --yes @mohtasham/md-to-docx docs/user/NAVOD-PRO-TESTERY.md docs/user/Casomira-navod-pro-testery.docx"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0",
    "xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz"
  },
  "devDependencies": {
    "@electron/rebuild": "^4.0.4",
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^24.0.0",
    "@types/react": "^19.2.15",
    "@types/react-dom": "^19.2.2",
    "@vitejs/plugin-react": "^5.2.0",
    "electron": "^42.0.0",
    "electron-builder": "^26.0.12",
    "electron-vite": "^5.0.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "typescript": "^6.0.3",
    "vite": "^7.3.3"
  }
}
